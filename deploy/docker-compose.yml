services:
  khanh-voice:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    restart: unless-stopped
    env_file:
      - ../.env
    volumes:
      - khanh-voice-data:/srv/app/data
    ports:
      - "127.0.0.1:8000:8000"

volumes:
  khanh-voice-data:
